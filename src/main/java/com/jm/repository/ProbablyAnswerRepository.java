package com.jm.repository;

import com.jm.model.ProbablyAnswer;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProbablyAnswerRepository extends JpaRepository<ProbablyAnswer, Long> {
}
